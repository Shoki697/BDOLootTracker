<Window x:Class="BDOLootTracker.Views.CloseChoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BDO Loot Tracker"
        Width="500"
        Height="250"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource Bg}"
        Foreground="{StaticResource Text}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="18" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Exit BDO Loot Tracker?"
                   FontSize="22"
                   FontWeight="SemiBold" />

        <Border Grid.Row="2"
                Background="{StaticResource Panel}"
                BorderBrush="{StaticResource Border}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="14">
            <TextBlock Text="You can close the tracker completely, return to the app, or keep the active session running in the system tray. The overlay stays available in Tray mode."
                       Foreground="{StaticResource Muted}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center" />
        </Border>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    Content="EXIT"
                    Background="#8E2D35"
                    Click="Exit_Click" />
            <Button Grid.Column="2"
                    Content="CANCEL"
                    IsCancel="True"
                    Click="Cancel_Click" />
            <Button Grid.Column="4"
                    Content="TRAY"
                    Background="#1266A8"
                    Click="Tray_Click" />
        </Grid>
    </Grid>
</Window>
